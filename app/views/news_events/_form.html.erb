<div class="container">
  <div class="row">
    <div class="form form-horizontal">
      <%= form_for([@news_event.university, @news_event]) do |f| %>
  <% if @news_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@news_event.errors.count, "error") %> prohibited this news_event from being saved:</h2>

      <ul>
      <% @news_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label class="col-md-2">Title</label>
    <input type="text" class="col-md-10" name="title">
  </div>
  <div class="form-group">
    <label class="col-md-2">Post_date</label>
    <input type="text" class="col-md-10" name="post_date">
  </div>
  <div class="form-group">
    <label class="col-md-2">Author</label>
    <input type="text" class="col-md-10" name="author">
  </div>
  <div class="form-group">
    <label class="col-md-2">Content</label>
    <input type="text" class="col-md-10" name="content">
  </div>
  <div class="actions">
    <button class="btn btn-success pull-right" type=submit>Submit</button>
  </div>
<% end %>
    </div>
  </div>
</div>

